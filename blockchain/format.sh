#!/bin/bash
clang-format -i --files=./clang-format block.cpp
clang-format -i --files=./clang-format block.hpp
clang-format -i --files=./clang-format blockchain.hpp
clang-format -i --files=./clang-format blockchain.cpp
clang-format -i --files=./clang-format hash.hpp
clang-format -i --files=./clang-format hash.cpp
clang-format -i --files=./clang-format Peer.cpp
clang-format -i --files=./clang-format Peer.hpp
clang-format -i --files=./clang-format main.cpp
